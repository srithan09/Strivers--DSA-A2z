#include<bits/stdc++.h>
using namespace std;
int main()
{
    //[{10,5},{8,7},{25,3},{27,1}] this is the vector in which we have  to sort  if sort(vector) is used the sorting will happen only based on first value
    vector<pair<int,int>>v;
    int n;
    cin>>n;
    int first,second;
    for(int i=0;i<n;i++)
    {
        cin>>first;
        cin>>second;
        
    }

}